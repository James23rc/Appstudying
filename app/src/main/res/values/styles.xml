<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- My application theme. -->
    <style name="AppTheme.Transparent" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/my_colorPrimary</item>
        <item name="colorPrimaryDark">@color/my_colorPrimaryDark</item>
        <item name="colorAccent">@color/my_colorAccent</item>
        <item name="android:windowBackground">@color/my_colorTransparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
